# Development and test dependencies for the integration.
# The runtime dependency (pydaitem) is declared in the manifest and installed by HA.

# Floor matches the minimum declared in hacs.json, so the tests run against the oldest
# version a user can install on. A lower floor once let a fresh install resolve an older
# homeassistant lacking an API the integration used, invisible locally because this
# environment already had a newer version installed. Keep the two in step.
homeassistant>=2026.9.1
pytest>=8.0
pytest-asyncio>=0.24
pytest-cov>=5.0
pytest-homeassistant-custom-component>=0.13.364
ruff>=0.6
mypy>=1.11
pre-commit>=3.7

# API client library, published on PyPI. Locally, prefer the editable install, which
# takes precedence over this line:
#   pip install -e ../pydaitem
pydaitem>=0.2.0
